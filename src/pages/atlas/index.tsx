import React from 'react';
import { Layout } from '../../components/Layout';

const Atlas: React.FC = () => {
    return (
        <Layout>
            <h1>Teste</h1>
        </Layout>
    );
}

export default Atlas;